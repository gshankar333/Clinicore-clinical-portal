PORT=4000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthcare_portal
DB_USER=postgres
DB_PASSWORD=postgres

# VULNERABLE (A02 / A07): weak, hardcoded-style secret checked into an example file.
# We will intentionally keep this weak for now — it becomes a real finding for
# SAST/secret-scanning later, and we'll compare it against a properly managed
# secret once we reach the "fix" pass.
JWT_SECRET=supersecret123
JWT_EXPIRES_IN=7d
